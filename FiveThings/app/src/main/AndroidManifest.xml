<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.maliros.fivethings">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GoatsFactActivity"></activity>
        <activity android:name=".ArabicFactActivity"></activity>
        <activity android:name=".CappuccinoFactActivity"></activity>
        <activity android:name=".FruitFactActivity"></activity>
        <activity android:name=".SportFactActivity"></activity>

        <provider android:name=".contentproviders.FactsProvider"
            android:authorities="com.maliros.fivethings.contentproviders.FactsProvider"
            android:exported="false" />

        <receiver android:name=".services.AlarmReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"></action>
            </intent-filter>
        </receiver>

        <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

        <service android:name="com.maliros.fivethings.services.DBService" android:enabled="true" android:exported="false"></service>
        <service android:name="com.maliros.fivethings.services.DailyAlarmService" android:enabled="true" android:exported="false"></service>

    </application>

</manifest>
